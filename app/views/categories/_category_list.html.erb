<div class="categories">
  <p>
    Categories:
    <% obj.categories.each do |category| %>
                <span class="badge">
                  <%= link_to category.name, category_path(category) %>
                </span>
    <% end %>
  </p>
</div>